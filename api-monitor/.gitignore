# API Monitor: project-level ignores
# (root .gitignore already excludes .env / __pycache__ / *.pyc)

# SQLite database (local only)
data/
*.db
*.db-journal

# Streamlit cache
.streamlit/secrets.toml

# Virtualenv (just in case)
.venv/
venv/
